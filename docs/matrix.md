# Matrix semantics

Implementing and verifying are separate claims. Coverage counts clauses with at least one accepted artifact; it says nothing about correctness. Uncovered clauses and orphan artifacts are review queues. Hash-based impact only identifies previously reviewed cells whose source changed.
